"use strict";
// Question88: Rounding to the nearest integer. Write a function that take a decimal number as input and returns the number rounded to the nearest integer.
// create a function that take decimal number
function roundingOfDecimal(decinumber) {
    return Math.round(decinumber);
}
console.log(roundingOfDecimal(6.7777));
console.log(roundingOfDecimal(4.333));
